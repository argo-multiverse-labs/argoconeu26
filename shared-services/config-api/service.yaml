apiVersion: v1
kind: Service
metadata:
  name: config-api
  namespace: shared-services
  labels:
    app: config-api
spec:
  selector:
    app: config-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
